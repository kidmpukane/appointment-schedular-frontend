import "../authStyles/authStyles.css";

const LandingPageCTA = () => {
  return (
    <section className="cta-section">
      <h1 className="cta-title">Hire reliable and trustworthy talents.</h1>
      <p className="cta-subtitle">
        Our landing page template works on all devices, so you only have to set
        it up once, and get beautiful results forever.
      </p>
      <button className="cta-button">Schedule A Call</button>
    </section>
  );
};

export default LandingPageCTA;
